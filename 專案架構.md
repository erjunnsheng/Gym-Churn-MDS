# 專案架構 — 健身房會員流失分析

> 一份地圖，讓任何讀者（你、老師、未來的你、合作同學）能在 5 分鐘內看懂整個專案是什麼、各檔案做什麼、彼此怎麼接。

---

## 1. 一句話總結

> 從 Kaggle 拿 4000 筆健身房會員資料，
> 用 **XGBoost** 預測流失 + **K-means** 分客戶 + **chocoZAP** 費率算 **CLV**，
> 做 **Feature Engineering V1 vs V2 對照實驗**（共線性 0.97 → 0.44），
> 產出 **互動式 Streamlit dashboard (V2)** + **4 個人物誌** + **17 張報告用圖**，
> 並用本作者自身 chocoZAP Cluster 0 經驗作真實案例對照。

---

## 2. 三個視角看整個專案

### 2.1 「資料」視角 — 一條資料流水線

```
┌──────────────────────────────────────────────────────┐
│  資料源 (Yandex Practicum)                            │
│  gym_churn_us.csv  ── 4000 列 × 14 欄                │
└────────────────────────┬─────────────────────────────┘
                         │
              ┌──────────┴──────────┐
              ▼                     ▼
       ┌────────────┐        ┌────────────┐
       │ train/test │        │   全資料    │
       │ 8:2 split  │        │ (for分群)   │
       └─────┬──────┘        └─────┬──────┘
             ▼                     ▼
       ┌────────────┐        ┌────────────┐
       │  XGBoost   │        │  K-means   │
       │ ROC≈0.97   │        │   K=4      │
       └─────┬──────┘        └─────┬──────┘
             │                     │
             ▼                     ▼
       ┌────────────┐        ┌────────────┐
       │   SHAP     │        │  4 群動態  │
       │  解釋性    │        │   命名     │
       └─────┬──────┘        └─────┬──────┘
             │                     │
             └──────────┬──────────┘
                        ▼
              ┌──────────────────┐
              │   CLV 計算       │
              │ (chocoZAP 費率)  │
              └────────┬─────────┘
                       │
       ┌───────────────┼──────────────┐
       ▼               ▼              ▼
 ┌─────────┐    ┌──────────┐   ┌──────────┐
 │ Notebook│    │ Streamlit│   │ Persona  │
 │ (.ipynb)│    │ (.py)    │   │ (.md)    │
 └────┬────┘    └────┬─────┘   └────┬─────┘
      │              │              │
      └──────────────┼──────────────┘
                     ▼
            ┌────────────────┐
            │ MDS 期末報告   │
            └────────────────┘
```

### 2.2 「使用者」視角 — 三條路徑

| 角色 | 想做什麼 | 開哪個檔案 |
|---|---|---|
| **分析師（你）** | 跑分析、調整參數、看結果 | `analysis.ipynb` |
| **行銷經理（demo 對象）** | 輸入會員特徵看預測 | `app.py`（Streamlit） |
| **報告作者（你）** | 抓圖、抓 persona、寫章節 | `figures/`、`personas.md`、本檔 |

### 2.3 「報告」視角 — 每個檔案對應哪一章

| 報告章節 | 對應檔案 / 段落 |
|---|---|
| 第二章 現況 | `personas.md` Cluster 0（你本人） |
| 第三章 資料產品 | 整個 pipeline + `app.py` |
| 第六章 4P · Product | `app.py` + `notebook` Sec 4-5 |
| 第六章 4P · Price | `notebook` Sec 7（CLV） |
| 第六章 4P · Promotion | `personas.md` 各群行動清單 |
| 第七章 技術 | `notebook` Sec 4 + Sec 7.5 + `figures/05-09, 15-17` |
| 第八章 STP | `notebook` Sec 6 + `personas.md` 全段 |
| 第十章 反思 | 各檔末尾「已知限制」 + §7.5 結論 |
| 個案研究章節 ⭐ | `notebook` Sec 7.4 + `personas.md` Cluster 0 商業洞察 |
| 附錄 / 實驗紀錄 ⭐ | `notebook` Sec 7.5 V1 vs V2 對照（共線性、ROC、SHAP、群結構） |

---

## 3. 完整檔案地圖

```
gym_churn/
│
├── 📊 資料層
│   └── data/
│       ├── gym_churn_us.csv             # 來源資料 4000×14（不要動）
│       ├── gym_churn_with_clusters.csv  # notebook 生成（加 cluster 欄）
│       └── cluster_profile.csv          # notebook 生成（4 群統計）
│
├── 🔬 分析層
│   ├── analysis.ipynb                   # 主分析（77 cells、含 §7.5 V1 vs V2 實驗）
│   ├── make_notebook.py                 # ↑ 的 Python source code
│   ├── experiment_v2.py                 # V1 vs V2 對照實驗腳本
│   ├── make_figures_v2.py               # 產生 figures_v2/ 的腳本
│   ├── xgb_churn_model.pkl              # V1 XGBoost 權重
│   └── xgb_churn_model_v2.pkl           # V2 XGBoost 權重（app.py 載入）
│
├── 🖼️ 視覺化層
│   ├── figures/                         # 17 張 PNG（直接放報告）
│   │   ├── 01-04  ── EDA 圖
│   │   ├── 05-06  ── 模型比較圖
│   │   ├── 07-09  ── SHAP 解釋圖
│   │   ├── 10-12  ── K-means 分群圖
│   │   ├── 13-14  ── CLV 圖
│   │   └── 15-17  ── V1 vs V2 對照圖（共線性、ROC、SHAP）
│   └── figures_v2/                      # 7 張 PNG（app.py 顯示用）
│
├── 🎯 應用層
│   ├── app.py                           # Streamlit 互動 dashboard（V2 模型）
│   └── personas.md                      # 4 人物誌（Cluster 0 = 你）
│
├── 📖 文件層
│   ├── README.md                        # 啟動 / 操作說明
│   ├── 專案架構.md                       # 本文件
│   └── requirements.txt                 # 套件鎖（118 個）
│
└── 🐍 環境層
    └── .venv/                           # Python 3.11.9 + 套件
```

---

## 4. 各檔案做什麼（深度版）

### 4.1 `data/gym_churn_us.csv`（來源）
- 4000 列 × 14 欄
- 來自 Yandex Practicum「Model Fitness」教學集
- 14 欄分：6 個二元（gender 等）+ 7 個數值（Age 等）+ 1 個目標（Churn）
- **完整無缺值、無重複**

### 4.2 `make_notebook.py`（notebook 的 source code）
- ~900 行 Python，定義 77 個 cells 後 `json.dump` 成 `.ipynb`
- 改分析流程從這裡改，**不要直接編輯 ipynb**
- 跑法：`python make_notebook.py` 就生 / 覆蓋 `analysis.ipynb`

### 4.3 `analysis.ipynb`（主分析）

| 章節 | 在做什麼 | 產出圖 |
|---|---|---|
| 1. 載入資料 | shape / info / describe / 缺值檢查 | — |
| 2. EDA | 流失分布、特徵 by churn、相關性 | 01-04 |
| 3. 預處理 | train/test split + StandardScaler | — |
| 4. 模型訓練 | Logistic / RF / XGBoost + ROC + CM | 05-06 |
| 5. SHAP | TreeExplainer + summary + dependence | 07-09 |
| 6. K-means | Elbow + 4 群側寫 + PCA + 流失率 | 10-12 |
| **7. CLV** ⭐ | chocoZAP 費率 → 各群 CLV → 預算 + 商業洞察 | 13-14 |
| **7.5 V1 vs V2** ⭐ | Feature engineering 對照實驗：共線性 0.97 → 0.44，AUC 持平 | 15-17 |
| 8. 商業建議 | 章節對應表 + 未來工作 | — |

### 4.4 `xgb_churn_model.pkl` / `xgb_churn_model_v2.pkl`（模型權重）
- 兩個版本的 Pickle 序列化 XGBoost
  - **V1**: 用 `Month_to_end_contract`（保留作為對照）
  - **V2**: 用 `months_into_current`（**app.py 載入這個**）
- 各約 280 KB

### 4.5 `app.py`（Streamlit dashboard，V2 模型）
- ~270 行 Python
- 啟動：`streamlit run app.py` → http://localhost:8501
- 載入 `xgb_churn_model_v2.pkl`，輸入用 `months_into_current`
- 功能：
  1. Sidebar 一鍵載入「我的 chocoZAP profile」
  2. 三欄表單輸入 13 個特徵（V2 schema）
  3. 「已過合約月數」滑桿，上限自動 = Contract_period（業務邏輯保證）
  4. 即時顯示流失機率 + cluster + CLV + 行銷預算
  5. 展開區嵌入 V2 SHAP / PCA / CLV 圖 + V1 vs V2 ROC 對照
  6. 偵測到 chocoZAP profile 時顯示特殊案例對照區塊

### 4.6 `personas.md`（4 人物誌）
- Cluster 0「我」：本作者真實 chocoZAP 訪談（吸引點 + 退會理由 + 商業洞察）
- Cluster 1 Amy：核心 VIP 代表
- Cluster 2 阿凱：活躍短期客代表
- Cluster 3 Eric：熄火族代表

### 4.7 `requirements.txt`
- 118 個套件 pip freeze 全量
- 主角：pandas、scikit-learn、xgboost、shap、matplotlib、seaborn、jupyter、streamlit

### 4.8 `experiment_v2.py`（V1 vs V2 對照實驗腳本）
- 獨立腳本，重訓三模型於 V2 特徵
- 輸出：metrics 對照、ROC 重疊、SHAP 比較、K-means 群側寫
- 跑法：`python experiment_v2.py`
- 同樣的對照已整合進 `analysis.ipynb` §7.5

### 4.9 `make_figures_v2.py`（產生 figures_v2/）
- 用 V2 模型生成 6 張圖給 `app.py` 顯示
- 跑法：`python make_figures_v2.py`
- 跟 §7.5 notebook 內的圖不同：notebook 圖在 `figures/15-17`，app.py 用 `figures_v2/v2_*`

---

## 5. 模型架構

### 5.1 流失預測（監督式分類）

```
Input  : 13 個特徵（gender, age, Lifetime, ...）
Model  : XGBoost (n_estimators=200, max_depth=4, lr=0.1)
Output : P(Churn=1) ∈ [0, 1]
Metric : ROC AUC ≈ 0.97
```

### 5.2 客戶分群（無監督）

```
Input  : 13 個特徵（含 Churn？不含 — KMeans 不用標籤）
Pre    : StandardScaler 全資料 fit_transform
Model  : KMeans(K=4, random_state=42, n_init=10)
Output : cluster id ∈ {0, 1, 2, 3}
```

### 5.3 模型可解釋性

```
Input  : XGBoost model + 測試集 X
Tool   : shap.TreeExplainer
Output : 每個預測的每個 feature 的 SHAP 值
        → summary plot：每特徵總影響力
        → dependence plot：特徵值 ↔ 流失機率
```

### 5.4 CLV 公式

```
SIGNUP_FEE   = 500    # chocoZAP 入會費
MONTHLY_FEE  = 1100   # chocoZAP 台北月費

historical_CLV  = SIGNUP_FEE + Lifetime × MONTHLY_FEE + Avg_additional_charges
future_revenue  = (1 - churn_prob) × Month_to_end_contract × MONTHLY_FEE
expected_CLV    = historical_CLV + future_revenue

留客預算上限  = expected_CLV × 20%（保守）or 30%（積極）
```

> V2 模型使用 `months_into_current` 後，CLV 仍用 M2E 計算（業務邏輯），
> 由 `M2E = Contract_period − months_into_current` 反推。

### 5.5 V1 vs V2 Feature Engineering 實驗（§7.5）

**動機**：V1 的 `Contract_period` 跟 `Month_to_end_contract` 相關係數 = **0.973**（病態多重共線性，84% 的會員 M2E = Contract_period）。

**處理**：將 `Month_to_end_contract` 替換成
`months_into_current = Contract_period − Month_to_end_contract`，
資訊等價但結構更清晰。

**結果（V1 → V2）**：

| 指標 | V1 | V2 | 變化 |
|---|---:|---:|---|
| Contract_period 共線性 | 0.973 | 0.438 | ✅ 大幅改善 |
| Logistic AUC | 0.9774 | 0.9774 | 持平 |
| Random Forest AUC | 0.9684 | 0.9692 | +0.0009 |
| XGBoost AUC | 0.9796 | 0.9809 | +0.0014 |
| K-means 群流失率 | 56/27/9/3% | 54/27/8/6% | 結構穩健 |
| 你 chocoZAP churn | 88.4% | 83.1% | V2 略保守 |

**結論**：選 V2 — `app.py` 已切換，`analysis.ipynb` §7.5 為完整實驗紀錄。

**寫進報告的洞察**：
> Feature engineering 的目的不只是「**提升 accuracy**」，
> 更多時候是「**讓模型可信、可解釋、可被行銷部信任**」。
> 本實驗中預測力幾乎沒變，但多重共線性從病態值改善到合理範圍 — 這就是有意義的工程貢獻。

---

## 6. 動態 cluster 命名邏輯（重要設計）

**問題**：K-means 的 cluster ID 是 0-3，但 ID **不一定**對應同一種客戶
（取決於 K-means 內部初始化）。

**解法**：用**流失率排序**動態命名，不 hardcode ID：

```python
churn_by_cluster = df.groupby("cluster")["Churn"].mean().sort_values(ascending=False)
ranked = churn_by_cluster.index.tolist()
names = {
    ranked[0]: "試水族",      # 流失率最高
    ranked[1]: "熄火族",      # 第二
    ranked[2]: "活躍短期",    # 第三
    ranked[3]: "核心 VIP",    # 流失率最低
}
```

→ 即使 K-means 重訓導致 ID 變化，命名仍然正確對應客戶類型。
→ Streamlit `app.py` 啟動時即時計算，永遠對齊。

---

## 7. chocoZAP 在專案中扮演的 3 個角色

### 7.1 CLV 費率基準
把抽象的 `Lifetime × 月費` 轉換成具體的「**這位客戶值 NT$ X**」。
不再是教科書例題，而是台灣現實市場的決策參考。

### 7.2 個案研究（本作者真實樣本）
你 2026/4 月底入會、5 月只去 2 次 = 模型預測命中的 Cluster 0 試水族真實樣本。
量化結果 + 質化訪談 = 老師「**競爭分析 2.0**」框架。

### 7.3 商業模式對照（核心洞察）
你給的退會理由揭露的矛盾：

```
行銷策略：用 KTV、按摩椅等差異化吸客
            ↓
營運現實：這些設施供給上限低
            ↓
新會員越多 → 預約越擠 → 失望越多 → 流失越快
            ↓
        負循環
```

這段是報告**個案研究章節**的核心發現，**模型告訴你 56% 流失，訪談告訴你「為什麼會 56%」**。

---

## 8. 三條使用路徑（操作）

### 路徑 A — 重跑完整分析

```powershell
cd C:\Users\cihci\Desktop\MDS\gym_churn
.\.venv\Scripts\Activate.ps1
jupyter notebook analysis.ipynb
# 在 Jupyter UI 按 Cell → Run All，約 40 秒
```

或純命令列：

```powershell
jupyter nbconvert --to notebook --execute analysis.ipynb --output analysis.ipynb
```

### 路徑 B — 啟動互動 demo

```powershell
cd C:\Users\cihci\Desktop\MDS\gym_churn
.\.venv\Scripts\Activate.ps1
streamlit run app.py
# 瀏覽器自動打開 http://localhost:8501
# 左側點「載入：我的 chocoZAP profile」測試
```

### 路徑 C — 改流程後重 build

```powershell
# 編輯 make_notebook.py（加 cell、調參數等）
.\.venv\Scripts\python.exe make_notebook.py
jupyter nbconvert --to notebook --execute analysis.ipynb --output analysis.ipynb
```

---

## 9. 報告章節 → 素材對應（完整版）

| 報告章節 | 素材出處 |
|---|---|
| 摘要 / 第一章 緒論 | README.md 的「一句話總結」 |
| 第二章 現況 | `personas.md` Cluster 0 開場故事 |
| 第三章 資料產品 | 本檔 §2.1 流水線圖 + `app.py` 截圖 |
| 第四章 文獻探討 | chocoZAP 經理人 / 天下雜誌引用 |
| 第五章 方法論 | 本檔 §5、§6 模型架構 |
| 第六章 4P · Product | XGBoost + SHAP + `app.py` |
| 第六章 4P · Price | `notebook` Sec 7 CLV 表 + `figures/13` |
| 第六章 4P · Place | （需自行補：chocoZAP 分店通用、APP 通路） |
| 第六章 4P · Promotion | `personas.md` 各群行動清單 + `notebook` Sec 7.3 |
| 第七章 技術實作 | `notebook` 全部 + `figures/05-12` + §7.5 V1 vs V2 (`figures/15-17`) |
| 第八章 STP | `notebook` Sec 6 + `personas.md` 全段 + `figures/11-12` |
| 第九章 商業建議 | `notebook` Sec 7.3 預算表 + `personas.md` 預算對照表 |
| **個案研究** ⭐ | `notebook` Sec 7.4 + `personas.md` Cluster 0 商業洞察 |
| **附錄 / 實驗紀錄** ⭐ | `notebook` Sec 7.5 V1 vs V2 對照（共線性、ROC、SHAP、群結構） |
| 第十章 反思 | 本檔 §11 + §7.5 結論 |
| 結論 | 本檔 §1 |

---

## 10. 技術棧

| 層 | 工具 |
|---|---|
| 語言 | Python 3.11.9 |
| 環境管理 | venv（`.venv/`） |
| 資料處理 | pandas 3.0、numpy 2.4 |
| ML 模型 | scikit-learn 1.8、xgboost 3.2 |
| 可解釋性 | shap 0.51 |
| 視覺化 | matplotlib 3.10、seaborn 0.13 |
| Notebook | jupyter 1.1、ipykernel 7.2、nbconvert 7.17 |
| 互動 app | streamlit |
| 字型 | Microsoft JhengHei（Win11 預設） |
| OS | Windows 11 |

---

## 11. 已知限制（也是反思章節）

| 限制 | 詳細 | 報告怎麼提 |
|---|---|---|
| 缺滿意度資料 | 只有行為，沒有 NPS / 退會問卷 | 第十章「資料盲點」 |
| 多重共線性（剩餘） | `Avg_class_frequency_total` ↔ `current_month`（V2 未處理） | 第七章模型解釋 |
| ✅ 多重共線性（已解決） | `Contract_period` ↔ M2E：V1 為 0.97，V2 改用 `months_into_current` 降到 0.44 | 第七章 §7.5 + 第十章 |
| 未做 uplift modeling | 沒實驗組資料，做不了因果推論 | 第十章「未來工作」 |
| Cluster 0 訪談 n=1 | 只有本作者，樣本量小 | 第十章「質性研究擴展」 |
| 教學集非真實營運資料 | Yandex Practicum 出版，數值合理但非實時 | 第四章資料來源說明 |
| CLV 簡化單一月費 | 沒處理不同方案組合、季節性 | 第十章模型擴展 |
| chocoZAP 費率單點 | 用 1100 元當基準，未做 sensitivity | 第十章模型擴展 |

---

## 12. 怎麼擴充

### 12.1 換資料集
- 換 `data/gym_churn_us.csv`（同樣 13+1 schema）
- 改 `make_notebook.py` 開頭路徑
- 重 build + 重執行

### 12.2 改 K-means K 值
- 改 `make_notebook.py` 中 Section 6.2 `K = 4`
- 重 build → 重執行
- `app.py` 動態命名自動 fallback（但要把 `CLUSTER_STRATEGY` 補上新群名）

### 12.3 換 chocoZAP 為其他健身房
- 改 `make_notebook.py` Sec 7 開頭 `MONTHLY_FEE` / `SIGNUP_FEE`
- 改 `app.py` 開頭同樣兩個常數
- `personas.md` 也手動改

### 12.4 加 uplift modeling
- 找實驗組資料（如 A/B test 結果）
- 用 `causalml` 或 `econml` 套件
- 在 Section 7 加入新 sub-section

### 12.5 Deploy Streamlit 到雲端
- 建 GitHub repo
- 連到 Streamlit Cloud（免費）
- 給老師 public URL

---

## 13. 一頁速查

```
資料  →  gym_churn_us.csv (4000×14)
模型  →  XGBoost (P churn) + K-means K=4 (cluster) + V2 改用 months_into_current
解釋  →  SHAP (feature importance) + V1 vs V2 對照
價值  →  CLV (chocoZAP 1100/月)
輸出  →  notebook (77 cells) + Streamlit (V2) + 4 personas + 17 figures
案例  →  本作者 chocoZAP 真實 Cluster 0 經驗
洞察  →  chocoZAP 商業模式的供需錯配 + V1→V2 特徵工程改善共線性
實驗  →  §7.5 完整 V1 vs V2 對照（共線性 0.97→0.44，AUC 持平）
報告  →  10 章 + 附錄實驗紀錄都齊
```
